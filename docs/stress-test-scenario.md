# Stress Test Scenario


## 1. Multi connection scenario sending two peers
multiple connections for multiple users/addresses and sending at the same time two peers

## 2. Multi connection scenario sending group
multiple connections for multiple users/addresses and sending at the same time to group

## 3. Flood the message queue
flood the message queue with messages to see how the system handles it

## 4. Flood the user creation
flood the user creation with users to see how the system handles it

## 5. Flood the open imap connection 
flood the open imap connection with messages to see how the system handles it
see if the messages still can be received
** use list of imap folders as functionality test

## 6. Flood the smtp connection for sending
flood the smtp connection with messages to see how the system handles it

## 7. create group chat of 100 people and verify a sent message arrives to all members
create group chat of 100 people and verify a sent message arrives to all members


# mesurements

- cpu usage / per time
- memory usage / per time
- number of messages received / per time
- number of messages sent / per time
- successful secure join / per time
- 


